public class GeneratedPath {

  public GeneratedPath() {
    PathBuilder builder = new PathBuilder();

    builder
      .addPath(
        // Line 1
        new BezierLine(
          new Point(10.000, 60.000, Point.CARTESIAN),
          new Point(35.000, 60.000, Point.CARTESIAN)
        )
      )
      .setConstantHeadingInterpolation(Math.toRadians(180))
      .addPath(
        // Line 2
        new BezierLine(
          new Point(35.000, 60.000, Point.CARTESIAN),
          new Point(30.000, 44.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(180), Math.toRadians(305))
      .addPath(
        // Line 3
        new BezierLine(
          new Point(30.000, 44.000, Point.CARTESIAN),
          new Point(30.000, 42.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(305), Math.toRadians(230))
      .addPath(
        // Line 4
        new BezierLine(
          new Point(30.000, 42.000, Point.CARTESIAN),
          new Point(30.000, 34.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(230), Math.toRadians(305))
      .addPath(
        // Line 5
        new BezierLine(
          new Point(30.000, 34.000, Point.CARTESIAN),
          new Point(30.000, 32.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(305), Math.toRadians(230))
      .addPath(
        // Line 6
        new BezierLine(
          new Point(30.000, 32.000, Point.CARTESIAN),
          new Point(30.000, 24.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(230), Math.toRadians(305))
      .addPath(
        // Line 7
        new BezierLine(
          new Point(30.000, 24.000, Point.CARTESIAN),
          new Point(27.000, 34.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(305), Math.toRadians(230))
      .addPath(
        // Line 8
        new BezierLine(
          new Point(27.000, 34.000, Point.CARTESIAN),
          new Point(26.000, 46.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(230), Math.toRadians(245))
      .addPath(
        // Line 9
        new BezierLine(
          new Point(26.000, 46.000, Point.CARTESIAN),
          new Point(35.000, 62.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(245), Math.toRadians(180))
      .addPath(
        // Line 10
        new BezierLine(
          new Point(35.000, 62.000, Point.CARTESIAN),
          new Point(26.000, 46.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(180), Math.toRadians(230))
      .addPath(
        // Line 11
        new BezierLine(
          new Point(26.000, 46.000, Point.CARTESIAN),
          new Point(35.000, 64.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(230), Math.toRadians(180))
      .addPath(
        // Line 12
        new BezierLine(
          new Point(35.000, 64.000, Point.CARTESIAN),
          new Point(26.000, 46.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(180), Math.toRadians(230))
      .addPath(
        // Line 13
        new BezierLine(
          new Point(26.000, 46.000, Point.CARTESIAN),
          new Point(35.000, 66.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(230), Math.toRadians(180))
      .addPath(
        // Line 14
        new BezierLine(
          new Point(35.000, 66.000, Point.CARTESIAN),
          new Point(26.000, 46.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(180), Math.toRadians(230))
      .addPath(
        // Line 15
        new BezierLine(
          new Point(26.000, 46.000, Point.CARTESIAN),
          new Point(35.000, 68.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(230), Math.toRadians(180))
      .addPath(
        // Line 16
        new BezierLine(
          new Point(35.000, 68.000, Point.CARTESIAN),
          new Point(15.000, 30.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(180), Math.toRadians(360));
  }
}

public class GeneratedPath {

  public GeneratedPath() {
    PathBuilder builder = new PathBuilder();

    builder
      .addPath(
        // Line 1
        new BezierLine(
          new Point(134.000, 84.000, Point.CARTESIAN),
          new Point(109.000, 84.000, Point.CARTESIAN)
        )
      )
      .setConstantHeadingInterpolation(Math.toRadians(0))
      .addPath(
        // Line 2
        new BezierLine(
          new Point(109.000, 84.000, Point.CARTESIAN),
          new Point(114.000, 100.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(0), Math.toRadians(135))
      .addPath(
        // Line 3
        new BezierLine(
          new Point(114.000, 100.000, Point.CARTESIAN),
          new Point(114.000, 102.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(135), Math.toRadians(50))
      .addPath(
        // Line 4
        new BezierLine(
          new Point(114.000, 102.000, Point.CARTESIAN),
          new Point(114.000, 110.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(50), Math.toRadians(135))
      .addPath(
        // Line 5
        new BezierLine(
          new Point(114.000, 110.000, Point.CARTESIAN),
          new Point(114.000, 112.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(135), Math.toRadians(50))
      .addPath(
        // Line 6
        new BezierLine(
          new Point(114.000, 112.000, Point.CARTESIAN),
          new Point(114.000, 120.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(50), Math.toRadians(125))
      .addPath(
        // Line 7
        new BezierLine(
          new Point(114.000, 120.000, Point.CARTESIAN),
          new Point(117.000, 110.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(125), Math.toRadians(50))
      .addPath(
        // Line 8
        new BezierLine(
          new Point(117.000, 110.000, Point.CARTESIAN),
          new Point(118.000, 98.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(50), Math.toRadians(45))
      .addPath(
        // Line 9
        new BezierLine(
          new Point(118.000, 98.000, Point.CARTESIAN),
          new Point(109.000, 82.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(45), Math.toRadians(0))
      .addPath(
        // Line 10
        new BezierLine(
          new Point(109.000, 82.000, Point.CARTESIAN),
          new Point(118.000, 98.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(0), Math.toRadians(50))
      .addPath(
        // Line 11
        new BezierLine(
          new Point(118.000, 98.000, Point.CARTESIAN),
          new Point(109.000, 80.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(50), Math.toRadians(0))
      .addPath(
        // Line 12
        new BezierLine(
          new Point(109.000, 80.000, Point.CARTESIAN),
          new Point(118.000, 98.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(0), Math.toRadians(50))
      .addPath(
        // Line 13
        new BezierLine(
          new Point(118.000, 98.000, Point.CARTESIAN),
          new Point(109.000, 78.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(50), Math.toRadians(0))
      .addPath(
        // Line 14
        new BezierLine(
          new Point(109.000, 78.000, Point.CARTESIAN),
          new Point(118.000, 98.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(0), Math.toRadians(50))
      .addPath(
        // Line 15
        new BezierLine(
          new Point(118.000, 98.000, Point.CARTESIAN),
          new Point(109.000, 76.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(50), Math.toRadians(0))
      .addPath(
        // Line 16
        new BezierLine(
          new Point(109.000, 76.000, Point.CARTESIAN),
          new Point(129.000, 114.000, Point.CARTESIAN)
        )
      )
      .setLinearHeadingInterpolation(Math.toRadians(0), Math.toRadians(180));
  }
}
